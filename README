# dwm - dynamic window manager
This repo contains my build of dwm.
The patches that are included can be found in the patches folder

My build of dwm also uses polybar with the anybar patch, and thus, polybar-dwm-module is a dependency.
